# feat-006 验收工单 — found-20260619-150623

独立 checker（脾气差小白视角）。验收对象：
1. feat-006：grill→loop/AFK 节奏并进两个现有产物（粘贴版清单 + skill）。
2. doc 修复：init.sh 头注 + CONTEXT.md 验收线 1（"init.sh 只验本仓库，别拿去小白文件夹跑"）。

真空实跑：`D:/build4me/.check-feat006-tmp/`，小白项目="记单词的小网页（单词卡）"，照粘贴版 prompt 当真生成 5 个文件。

---

## 逐条验收线结论

- **A**（清单里 grill→loop 既进"写给 AI 的指令"又给小白一句可照说的话）：**PASS**
  - 指令：`四件套起步清单.md:31-34`（"再加一条'干活的节奏'规矩…先像采访一样把我要的东西问清楚…计划定了，再进 maker→checker 循环"）。
  - 小白可照说：`四件套起步清单.md:64-73` 灰框（"先别写代码。先把这个功能问清楚我到底要啥…然后你写、自己换身份挑错改"）。
- **B**（真空跑出的 AGENTS.md 里确实有这条节奏、针对项目、说人话）：**PASS**
  - `.check-feat006-tmp/AGENTS.md` 的 "## 干活的节奏" 段：先问清楚（像采访，举了"复习按时间隔还是手动挑"的项目例）→ 写代码并自己换身份挑错打回 → 改不动才回来问。零裸黑话，全大白话，针对单词卡。
- **C**（SKILL.md 并进同一条：step 2 AGENTS 内容 + step 3 收尾示范）：**PASS**
  - step 2：`SKILL.md:27`（"再加一条 干活节奏规矩…先像采访一样把用户要的东西问清楚（grill…）…再进 maker→checker 循环…loop / AFK…用大白话写这条，别写成方法论长文"）。
  - step 3：`SKILL.md:42`（"把'先 grill 再 loop'的节奏顺手教给他"，并给了可照说示范句）。
- **D**（极简红线，没膨胀成长教程）：**PASS**
  - 清单仍 76 行、一屏量级；新增只是 prompt 框里 4 行 + 收尾 10 行。SKILL 只在已有 bullet 里加从句，没开新章节。流程仍是"问一句→建文件→几行解释→给下一步"。
- **E**（说人话红线，grill/loop/AFK/maker/checker 不裸甩）：**PASS**
  - 小白直面的粘贴版清单里 **grill / loop / AFK 三个词压根没出现**（节奏全用大白话讲）；只有 maker/checker，且首次出现就内联解释（`:28-29` "一个负责写（maker）/另一个单独负责检查、挑错、打回（checker）"）。
  - SKILL.md（受众是 AI 不是小白）里 grill 有内联解释（`:27` "（grill：一次一两个关键问题…）"），maker/checker 有解释，loop/AFK 作为"放手让 AI 自己跑、不用盯每一步"的括注标签出现，意思已用大白话给出。
- **F**（dogfood：教人说人话/别长教程，自己没自相矛盾）：**PASS** —— 两个产物都极简、都用大白话，没把节奏写成方法论。
- **G**（doc 修复说清"init.sh 只验本仓库"、无残留误导）：**PASS**
  - `init.sh:2-6` 头注明写"只验 fourpack 本仓库自己…【不是】给小白产出物用的…别拿这个脚本去他的文件夹跑，会一片 FAIL"。
  - `CONTEXT.md:33`（"不是要小白去跑"）+ `:35`（"init.sh 只验本仓库…别拿 init.sh 去小白的文件夹跑…小白产出物靠人眼/清单核对"）。
  - 残留检查：粘贴版清单全文搜 `init` 0 命中 —— 不会把小白引去跑 init.sh。干净。
- **H**（回归）：**PASS**
  - `bash init.sh` 仍 PASS（EXIT=0），9 个必需文件齐全。
  - 上一轮 roles.md（fourpack dev 6 人桌）仍在、未被改坏；粘贴版仍生成 5 个文件（AGENTS/CONTEXT/progress/session-handoff + roles.md），真空实跑已证。
  - 说明：本轮 prompt 提到"上一轮 roles.md（3 人小圆桌）"——实际 `D:/build4me/roles.md` 是 fourpack 自身 dev 6 人桌（feat-005 evidence 明写"fourpack 自身 dev roles.md 不瘦身保留 6 人桌"）；3 人桌是粘贴版让 AI 生成给小白的那个。两者各自正确，没冲突。

---

## 三层分诊

### 硬契约（文件缺失 / init.sh 不过 / 节奏没进某产物 / 生成 AGENTS.md 没这条 / 明显长教程）
- 无。两个产物都进了节奏、真空 AGENTS.md 有这条、init.sh PASS、无膨胀。

### 疑似（需要 Danny 拍 / 可能算坑）
1. **feat-006 在 feature_list.json 里仍是 `not-started`、evidence 只有"Danny 提出"** —— 文件：`feature_list.json:44-50`。
   工作已经做完并进了两个产物，但状态台账没回写。按本项目 Definition of Done（`AGENTS.md`：feature 算完成必须 feature_list.json evidence 写清"日期+做了什么+怎么验+gate+下一步"），这条**还差最后一步回写**。内容验收过、但流程 DoD 未闭环。建议 maker 把 status 改 `done`、补 evidence（含本工单路径）。
   （归"疑似"不归硬契约：硬契约定义是"节奏规矩压根没进产物 / 生成 AGENTS.md 没这条"，那些都满足了；这是台账滞后，不是内容缺失。）

### 质感（不打回、记账）
1. SKILL.md `:27` 一条 bullet 里塞了 grill + maker/checker + loop/AFK 三组词，单行偏长、信息密。受众是 AI 不是小白，可接受；但若想更稳，可把 loop/AFK 那半句再口语化一点（如"放手让 AI 自己连着跑几轮"）。小事。
2. 真空生成的 AGENTS.md "干活的节奏"写得比母版还顺——这是好事，记一笔：粘贴版 prompt 的措辞确实能逼出合格产物。

---

## 总判

**PASS（打回项=0 硬契约失败）。**

feat-006 的内容要求 A~G 全部满足，H 回归全绿，真空实跑的 AGENTS.md 确实长出了"先 grill 问清楚 → 再 maker/checker 循环 / 放手跑"这条说人话的节奏规矩，doc 修复也把"init.sh 只验本仓库"说清了、无残留误导。

唯一收尾欠账（非硬契约）：`feature_list.json` 里 feat-006 仍 `not-started`、缺 evidence，按本项目 DoD 需回写后这个 feature 才真正闭环。建议 maker 收尾时一并补上。

真空证据留存：`D:/build4me/.check-feat006-tmp/`（5 个文件）。
