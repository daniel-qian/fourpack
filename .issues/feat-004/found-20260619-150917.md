# Checker findings — feat-004 English starter checklist

- Checker: independent (not the author)
- Object: `four-pillars-starter-checklist.md` vs master `四件套起步清单.md`
- Method: full read of both files; line-by-line content alignment; real-run of the gray-box prompt in `.check-feat004-tmp/` posing as the AI with beginner project "a tiny webpage to track books I've read"; `bash init.sh` regression.

---

## Layer 1 — Hard contract (blocking)

None. All blocking lines pass:

- **Content coverage (A):** every block of the master is present in the English version — 3-step how-to, paste-able gray-box prompt, five generated files (AGENTS/CONTEXT/progress/session-handoff + roles.md), maker/checker rule, grill→loop "rhythm of working" rule, roles.md three people (PM / UI-UX / viewer, viewer = checker), the "one sentence to remember while you work" closing block, and the "this checklist was made with this exact method" sign-off. Nothing missing or shrunk.
- **Hard instructions in the prompt (B):** all four critical instructions survived translation —
  - "ask me only one thing first, then stop and wait" → EN lines 22-23 ✓
  - "write each file specifically for the project I described — no generic templates" → EN lines 25-26 ✓
  - "explain a technical term in one sentence the first time it shows up" → EN lines 17-18 ✓
  - "pick the simplest sensible default at decision points" → EN lines 19-20 ✓
- **Real-run (B):** the English gray-box prompt, run cold, produced 5 clean files (4 pillars + a 3-person roles.md). AGENTS.md contained the rhythm rule, all files were project-specific and in plain language. Prompt is self-contained and usable by an English beginner.
- **init.sh regression (G):** PASS (exit 0) both before and after the new file exists. init.sh's `required` list does not include the new English file, so adding it does not turn the baseline red. Confirmed not broken.

## Layer 2 — Suspected (translation drift / un-explained jargon)

- **maker/checker explanation is lighter than the Chinese (D).** EN line 32-34: "split the work into two roles — one writes (maker), the other separately checks, finds problems, and sends it back (checker)". This does gloss both terms inline, so it clears the bar. But the Chinese "别让同一个你既写又夸自己对" lands harder than EN "don't let the same you both write the work and pat itself on the back" — "the same you" is a slightly awkward carry-over of 同一个你. Minor. Severity: low.
- **"rhythm of working" vs grill (D).** Good call: the English never exposes the word "grill" to the beginner — it says "interview me to pin down what I actually want", which is the plain-language form. No un-explained jargon (no AFK / harness / loop-as-jargon leaks either). The closing block reuses "interview" consistently. Clears the bar. Severity: none (noted as a pass).
- **"you can reliably pick back up later" (C).** EN line 16 "a project 'you can reliably pick back up later'" reads slightly clunky as a quoted noun phrase; master 你以后能稳定接手 is smoother in CN. Acceptable but a hair stiff. Severity: low.

## Layer 3 — Feel / tone / rhythm

- **Generally idiomatic (C).** Reads as natural native English, not translationese. "grade your own homework" / "pat itself on the back" / "switch hats" are good idiomatic choices that match the master's casual register.
- **"the same you" (line 34)** is the one phrase that reads as a literal carry of 同一个你. Suggest "don't let one pass both write the work and pat itself on the back" or "don't let the writer also grade itself". Severity: feel.
- **Brand (F):** title "Four Pillars Starter Checklist (fourpack)" ties to the video brand cleanly. roles.md is still framed as "one small roundtable on the side" / "Plus one small roundtable" (line 69) — NOT promoted to a fifth pillar. The four pillars are explicitly the core. Brand framing intact.
- **dogfood (E):** no GPT-voice, no emoji spam, no empower/unleash/revolutionize. The closing "Not a slide deck — it's actually running." mirrors the master and stays in voice. The page is short and plain — it practices what it preaches.

---

## Top 3 worth fixing

1. **Line 34 "the same you"** — only phrase that reads as literal translation of 同一个你. Reword to e.g. "don't let one pass both write the work and grade itself." (Layer 3)
2. **Line 16 "a project 'you can reliably pick back up later'"** — slightly stiff quoted phrase; consider "a project you can reliably pick back up later" without forcing it into quotes, or "a project the AI can reliably resume." (Layer 2/3)
3. **maker/checker gloss** — optional: sharpen the inline explanation so a first-timer is 100% sure maker = writer / checker = separate reviewer (the Chinese contrast is a touch crisper). (Layer 2)

---

## Verdict: PASS

No hard-contract failures. Content fully aligned with the master, the gray-box prompt is self-contained and produced 5 clean files cold (including a 3-person roles.md and the rhythm rule in AGENTS.md), English is idiomatic, jargon is explained or avoided, brand framing holds (roles.md stays a side roundtable, not a fifth pillar), and init.sh still PASS. The three items above are polish, not blockers.
