# feat-005 checker 工单 — 真空文件夹实跑

- 时间：2026-06-19 14:52
- 验收对象：feat-005（生成物多出极简 3 人 roles.md：PM / UI/UX / viewer）
- 方法：在 `D:\build4me\.check-feat005-tmp\` 全新空文件夹里，**扮 AI 照 `四件套起步清单.md` 灰框 prompt 实跑**。
  - 自答那句"想做啥项目" = **"做一个记录我每天喝了几杯水的网页"**。
  - 严格照 prompt 建出 5 个文件：AGENTS.md / CONTEXT.md / progress.md / session-handoff.md / roles.md。
- 跑出来的证据文件留在 `.check-feat005-tmp\`（5 个文件都在）。

---

## 硬契约（机器定罪）

1. **【通过】roles.md 正好 3 个角色** —— `.check-feat005-tmp\roles.md` 三个二级标题：`## PM`、`## UI/UX`、`## viewer`。没偷塞第四个、没照搬母版多人桌。
2. **【通过】无占位符残留** —— 5 个文件全文未见 `___`（除 progress.md 模板里"下一步是 ___"那种本就该留的提示位，且已被实际内容替换）、未见 `<项目名>` / `TODO填` 之类未替换占位。文件里通篇是"喝水""一杯""加一杯""今天的杯数"等真实名词。
3. **【通过 / 但需说明】repo 自身 `cd D:\build4me && bash init.sh` = PASS**（实跑两次都绿）。
   - ⚠️ 说明：`init.sh` 校验的是 **build4me 仓库自己**（相对当前目录），**不是**真空文件夹。真空文件夹里只有粘贴版 prompt 要求的 5 个文件，**缺 CLAUDE.md / feature_list.json / SKILL.md / 四件套起步清单.md**，所以真空文件夹本身跑 init.sh 会 FAIL。但灰框 prompt 从没让小白产出这些，也没让他跑 init.sh，故不算 feat-005 的硬契约失败。归到下面"疑似"让人定夺。

→ **硬契约：无失败项。**

## 疑似（机器标记，人定夺）

1. **init.sh ↔ 粘贴版产出对不齐**（文件：`init.sh` line 9-19 vs `四件套起步清单.md` line 22-39）。
   - init.sh 的 `required` 列了 9 个文件（含 CLAUDE.md / feature_list.json / SKILL.md / 四件套起步清单.md）。但小白照粘贴版只会得到 5 个。**CONTEXT.md 第 33 行写的验收线 1 是"文件齐 + init.sh 过"**——一个小白如果照着这句话去自己文件夹跑 init.sh，会得到一片 FAIL，体验崩。
   - 严重度：中。这是验收线的语义裂缝：init.sh 只适合验 dogfood 仓库，不适合验小白产出。要么 CONTEXT 里写清"init.sh 只验本仓库"，要么给小白产出单独一套更轻的核对。**人定夺**。
2. **roles.md 里 viewer 当 checker 的指向，依赖 AGENTS.md 用词一致**（`.check-feat005-tmp\roles.md` 末行 "上面 AGENTS.md 里说的那个 checker…就让这个 viewer 来当"）。我生成的 AGENTS.md 里确实有 maker/checker 段，对得上。但"checker"是个英文工程词，AGENTS.md 里第一次出现时给了中文解释（"verit的/挑错打回"），roles.md 里又出现一次 **没有就地再解释**。小白跳读 roles.md 可能不懂 checker。严重度：低。**人定夺**该不该在 roles.md 里也补半句。
3. **PM/UI/UX/viewer 名字本身是英文角色词**，没翻译。小白未必知道 PM = 产品经理。三个标题后面都跟了中文括注（"产品把关""好不好用把关""挑剔的第一次用户"），算补救到位，但标题第一眼仍是英文缩写。严重度：低。**人定夺**。

## 质感（只描述，不判过）

- roles.md 短、一眼读完（约 30 行），节奏是"问一句→建文件→几行解释→下一步"，没膨胀成教程——符合 feat-005 E 线的预期。
- 三个角色都长在"喝水网页"上：PM 举例砍"顺便加个体重记录/登录"，UI/UX 盯"加一杯按钮够不够大""今天几杯有没有大数字"，viewer 问"下次还想不想再打开它来记"。是针对项目、不是通用套话（B 线达成）。
- 品牌没被冲淡：roles.md 开头明写"四件套之外顺带附的一个小圆桌，不是第五件套"，收尾说明也把它摆成附属（D 线达成）。
- 说人话方面：唯一冒出来的项目技术词 localStorage 在 CONTEXT.md 第一次出现就有整句大白话解释（"浏览器自带的小本子…关掉再打开还在…缺点是换台电脑就看不到"），roles.md 里零工程过程词（没有 harness / context engineering）。dogfood 红线 F：自身没自相矛盾。
- 一个观感小刺：viewer 设定"脾气还有点急"，对小白是亲切的，但跟 PM/UI/UX 的平铺语气略不一致——纯口味，不判。

---

## 总判：PASS

理由：三条 MVP 验收线 + feat-005 六条专项线（A~F）实跑全部达成；硬契约无失败项（roles.md 正好 3 角色、无占位符、repo init.sh 绿）。
保留两条**中/低严重度疑似**给人定夺，最重的一条是 **init.sh 的 required 清单与粘贴版小白产出对不齐**（CONTEXT.md 验收线 1 的措辞会误导小白去跑 init.sh）——不阻塞 feat-005，但建议下一轮顺手澄清。
